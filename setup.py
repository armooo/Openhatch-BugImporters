from setuptools import setup, find_packages

version = '0.11.09'

setup(name="bugimporters",
      version=version,
      author='OpenHatch development team <http://lists.openhatch.org/mailman/listinfo/devel>',
      license='AGPLv3',
      packages=find_packages(),
      install_requires=[])
